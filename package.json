{
    "name": "2a",
    "version": "1.0.8",
    "description": "",
    "main": "./index.js",
    "scripts": {
        "build": "tsc --project ./tsconfig.json",
        "prepublish": "[ $(echo $PWD | sed 's/\\// /g' | awk '{print $NF}') = 'dist' ] || (echo 'Must publish from dist directory! Try \"npm run dist\"' && exit 1)",
        "dist": "./publish.sh",
        "lint": "tslint --project ./tsconfig.json",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "git+ssh://git@github.com/neoscrib/2a.git"
    },
    "author": "Tanner Jepsen (neoscrib@gmail.com)",
    "license": "MIT",
    "bugs": {
        "url": "https://github.com/neoscrib/2a/issues"
    },
    "homepage": "https://github.com/neoscrib/2a#readme",
    "typings": "./index.d.ts",
    "devDependencies": {
        "@types/chai": "^4.2.15",
        "@types/mocha": "^8.2.0",
        "chai": "^4.3.0",
        "mocha": "^8.2.1",
        "ts-node": "^9.0.0",
        "tslint": "^6.1.3",
        "tslint-microsoft-contrib": "^6.2.0",
        "typescript": "^4.0.3"
    }
}
